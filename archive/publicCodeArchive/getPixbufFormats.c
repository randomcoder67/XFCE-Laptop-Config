	//GSList* formats = gdk_pixbuf_get_formats();
	//for (int i=0; i<17; i++) {
		//printf("%d\n", g_slist_length(formats));
		//GdkPixbufFormat* data = formats->data;
		//char* name = gdk_pixbuf_format_get_name(data);
		//printf("%s\n", name);
		//formats = formats->next;
	//}
	
	//char* arrayA[] = {"First item", "Seconds item", "Third item"};
	
	//currentGames = arrayA;
	
	//testA();
	
